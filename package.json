{
  "name": "smart-city-thailand-monitor",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run build -w packages/shared && npm run dev -w apps/web",
    "dev:api": "npm run build -w packages/shared && npm run dev -w apps/api",
    "dev:worker": "npm run build -w packages/shared && npm run dev -w apps/worker",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/worker && npm run build -w apps/web",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w apps/api && npm run typecheck -w apps/worker && npm run typecheck -w apps/web"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
